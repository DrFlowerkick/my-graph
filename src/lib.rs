//!lib.rs


// Add fn() for removing nodes and edges
// Implement basic structs for edges and meta data
// Create Tree struct

pub mod core;
pub mod base;

// at the Moment everything interesting is packed into modules
// probably I will only reexport interesting stuff in lib.rs